<Window x:Class="TravelAgency.WPF.Views.MessageDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Message"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        Topmost="True"
        WindowStyle="None">

    <Grid>
        <!-- Scrim for visibility -->
        <Rectangle Fill="#990B1220"/>

        <Border CornerRadius="18"
                Padding="18"
                MinWidth="560"
                BorderThickness="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Background>
                <SolidColorBrush Color="#111827" Opacity="0.92"/>
            </Border.Background>
            <Border.BorderBrush>
                <SolidColorBrush Color="White" Opacity="0.18"/>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect BlurRadius="38" ShadowDepth="14" Opacity="0.35" Color="#000000"/>
            </Border.Effect>

            <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Width="46" Height="46" CornerRadius="14" Background="#243B82F6" VerticalAlignment="Top">
                <Viewbox Width="22" Height="22" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Fill="#BFDBFE"
                          Stretch="Uniform"
                          Data="M12,2 C6.48,2 2,6.48 2,12 C2,17.52 6.48,22 12,22 C17.52,22 22,17.52 22,12 C22,6.48 17.52,2 12,2 Z M11,10 L13,10 L13,17 L11,17 Z M11,7 L13,7 L13,9 L11,9 Z"/>
                </Viewbox>
            </Border>

            <TextBlock Grid.Column="1"
                       Text="{Binding TitleText}"
                       Foreground="White"
                       FontWeight="SemiBold"
                       FontSize="18"
                       Margin="14,2,28,0"/>

            <TextBlock Grid.Row="1" Grid.Column="1"
                       Text="{Binding MessageText}"
                       Foreground="#E2E8F0"
                       Opacity="0.95"
                       FontSize="14"
                       TextWrapping="Wrap"
                       Margin="14,10,28,0"
                       LineHeight="20"
                       MaxWidth="560"/>

            <StackPanel Grid.Row="2" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="OK"
                        Width="110"
                        Height="40"
                        Padding="14,0"
                        Foreground="White"
                        Background="#52FFFFFF"
                        BorderBrush="#66FFFFFF"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="Ok_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Root" Property="Background" Value="#6AFFFFFF"/>
                                    <Setter TargetName="Root" Property="BorderBrush" Value="#80FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Root" Property="Background" Value="#3DFFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

